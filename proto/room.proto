syntax = "proto3";
package rpc;

import "user.proto";

service RoomHandler {
  rpc GetRoom(Room)returns(Room){}
}

message RoomName {
  string name = 1;
}

message Room{
  string name = 1;
  repeated rpc.User users = 2;
}

